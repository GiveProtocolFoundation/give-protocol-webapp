// Mock for @/components/settings/SetPasswordSettings (component, not hook)
// Used as a stub for the settings sub-panel when testing parent pages.
export const SetPasswordSettings = () => (
  <div data-testid="set-password-settings">Set Password Settings</div>
);
